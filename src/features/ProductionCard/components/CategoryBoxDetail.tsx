

export default function CateogoryBoxDetail({x,y}:{x:number,y:number}) {
    return(<>
        <div id="categoryboxdetail" className={`absolute top-[${y}px] left-[${x}px] border z-50`}>
            data
        </div>
    </>)
}